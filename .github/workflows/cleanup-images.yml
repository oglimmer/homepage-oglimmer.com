name: Cleanup container images

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  packages: write

jobs:
  cleanup:
    name: Delete untagged image versions
    runs-on: ubuntu-latest
    steps:
      - uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          owner: oglimmer
          packages: homepage-oglimmer-2025
          delete-untagged: true
